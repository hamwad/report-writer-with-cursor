{
  "name": "vue-frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173 --strictPort",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "gen:client": "encore gen client --output=./src/lib/client.ts --env=local"
  }
}
